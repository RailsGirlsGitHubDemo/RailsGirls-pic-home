<%= form_for( @photo ) do |f| %>
  <div id="upload_file">
    <button class="upload_button">Upload photo</button>

	
    <input id="file_photo" value="<%= f %>" required>

    <%= f.file_field :picture, { id: "upload"} %>
  </div>
<% end %>
